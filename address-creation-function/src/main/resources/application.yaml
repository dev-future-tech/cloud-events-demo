spring:
  application:
    name: address-creation-function
  cloud:
    stream:
      bindings:
        processAddress-in-0:
          destination: process-address-in
          group: process-address-group
        processAddress-out-0:
          destination: process-address-out
          group: process-address-group
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /
